# Pinned environment for the RiskLabAI Python tutorial notebooks.
# Mirrors Notebooks.jl/Project.toml: the local package is installed editable
# from a relative path, scientific deps are pinned for reproducibility.
#
#   python -m venv .venv && . .venv/Scripts/activate   # (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#
# FRED-backed notebooks read the FRED_API_KEY environment variable (fredapi).

-e ../RiskLabAI.py

numpy==1.26.4
pandas==2.2.3
scipy==1.11.4
scikit-learn==1.2.2
matplotlib==3.7.5
statsmodels==0.14.5
fredapi==0.5.2
torch==2.8.0
jupyter
nbconvert
